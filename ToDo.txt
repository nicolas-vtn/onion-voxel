
		----- ONION - VOXEL -----

		----- ENHANCEMENTS -----
- Add Sounds / Musics
- Render non-full blocks (water surface, cactus, ...)
- Animated textures (water, lava, ...)
- Better collision system (swept AABB)
- Add a "sneaking" state
- Add a "Creative" UI with Block selection and inventory management
- Add Player position interpolation in multiplayer
- Add a Chat system
- Add Player Names display on top of head in Multiplayer
- Add Player list display (tab)
- Add UI Tooltip on hovering something
- Particules (breaking blocks, footsteps, ...)
- First person view : Arm and selected block. (+ Movement on walking, placing and breaking)
- Use Block Model for collision instead of full block.


		----- KNOWN ISSUES -----
- Z-Ordering issue in GUI (Fix TextField Background drawn in wrong order to bypass this issue )
- On server close, client does not go back to main menu.
- Random crashes when loading worlds (probably due to server port)
- For ViewFrustum culling : use the real height of the chunk instead of a 256 height hard coded (BAD !) ...
- BlockStateJson fallback on default resourcepack if invalid BlockStateJson read from current resourcepack
- Add new blocks to "BlockState.cpp" (transparent / solid / flower)
- Fix Model uv and from / to usage. from / to defines the position where the textude should be drawn, and uv defines what part of the texture should be drawn.
- Implement Multi model faces. (Example a  Model with 2 "#west" faces.
